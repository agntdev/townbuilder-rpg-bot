# fix-358533c86d6abecb — Fix: PRIOR_DOC specifies the 'left_chat_member' event handler optionally logs and notes town record is dropped only on explicit '/reset' (out of v1 scope), but CURRENT_DOC's 'my_chat_member' handler only logs and does not mention the '/reset' scope limitation.

**Weight:** 0.0000 (share of project budget)
**Reward:** 0 TOWN

missing: PRIOR_DOC specifies the 'left_chat_member' event handler optionally logs and notes town record is dropped only on explicit '/reset' (out of v1 scope), but CURRENT_DOC's 'my_chat_member' handler only logs and does not mention the '/reset' scope limitation.

Severity: medium.