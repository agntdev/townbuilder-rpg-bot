# fix-83d4d495ba35f356 — Fix: PRIOR_DOC specifies the '/spend' flow uses a session step 'awaiting_building', but CURRENT_DOC adds a requirement to persist the menu message id in session for editing, which is not mentioned in PRIOR_DOC.

**Weight:** 0.0000 (share of project budget)
**Reward:** 0 TOWN

missing: PRIOR_DOC specifies the '/spend' flow uses a session step 'awaiting_building', but CURRENT_DOC adds a requirement to persist the menu message id in session for editing, which is not mentioned in PRIOR_DOC.

Severity: medium.