# fix-d7f8c8fcba613155 — Fix: PRIOR_DOC specifies the 'message' handler filters for non-bot messages using 'from.is_bot' and 'message.from.is_bot', but CURRENT_DOC only uses 'ctx.from?.is_bot' and adds a 'service' message filter not in PRIOR_DOC.

**Weight:** 0.0000 (share of project budget)
**Reward:** 0 TOWN

missing: PRIOR_DOC specifies the 'message' handler filters for non-bot messages using 'from.is_bot' and 'message.from.is_bot', but CURRENT_DOC only uses 'ctx.from?.is_bot' and adds a 'service' message filter not in PRIOR_DOC.

Severity: medium.